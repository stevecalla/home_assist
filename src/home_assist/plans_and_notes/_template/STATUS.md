# STATUS — <module>

Snapshot of what's built and what's next. Platform-level status is in `../STATUS.md`.

Last updated: <date>

## What it is

One paragraph: what this module watches or does, and which processes are involved.

## Routes

| path | panel | what |
|---|---|---|
| `/<id>/...` | `<id>` | |
| `/<id>/settings` | `<id>-admin` | |

## Done

What exists and works. Be specific about the pure/testable parts — those are the ones that will
still be trustworthy in six months.

## Verified

What was actually run, not what was written. Distinguish "tests pass" from "exercised end to end".

## Needs hardware / your machine

The things that cannot be checked from an agent env. Be blunt about these; an unverified claim in a
STATUS file is worse than an admitted gap.

## Open items

Numbered, so they can be referred to. Include the ones that need real-world data before they can be
answered.

## Ideas, not commitments

Explicitly separated from open items, so a future session doesn't treat a musing as a promise.
